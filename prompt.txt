I want to convert this existing 2048 web game into a fully native mobile application using Expo + React Native + TypeScript.

Do not create a WebView wrapper. Rebuild the game as a real mobile app while preserving the core 2048 gameplay mechanics.

Project requirements:

TECH STACK:
- Expo (latest stable version)
- React Native
- TypeScript
- Expo Router for navigation
- Zustand for state management
- React Native Reanimated for animations
- React Native Gesture Handler for swipe controls
- AsyncStorage for saving progress locally
- Expo Audio for sound effects
- Follow modern React Native best practices

GAME TRANSFORMATION:
Convert the existing web-based game logic into reusable TypeScript modules.

Separate the project into:

src/
 ├── app/
 │    ├── index.tsx
 │    ├── game.tsx
 │    └── settings.tsx
 │
 ├── components/
 │    ├── Board.tsx
 │    ├── Tile.tsx
 │    ├── ScoreBoard.tsx
 │    ├── Button.tsx
 │    └── Modal.tsx
 │
 ├── game/
 │    ├── engine.ts
 │    ├── moves.ts
 │    ├── merge.ts
 │    ├── random.ts
 │    └── types.ts
 │
 ├── store/
 │    └── gameStore.ts
 │
 ├── hooks/
 │    └── useGame.ts
 │
 ├── assets/
 │
 └── utils/

GAME FEATURES:
Implement:

- 4x4 grid gameplay
- Swipe controls:
  - Swipe left
  - Swipe right
  - Swipe up
  - Swipe down
- Tile merging logic
- Random tile spawning
- Score calculation
- High score tracking
- Win condition
- Game over detection
- Restart game functionality
- Undo previous move
- Persistent save system

MOBILE UI:
Design a modern mobile-first interface.

Requirements:
- Responsive layout for different phone sizes
- Smooth tile animations
- Tile appearing animation
- Tile merging animation
- Smooth board movement
- Dark mode support
- Clean modern design
- Touch-friendly buttons

HOME SCREEN:
Create:

- Game logo/title
- Play button
- Continue game button
- High score display
- Settings button
- Statistics button

GAME SCREEN:
Include:

- Current score
- Best score
- Game board
- Restart button
- Undo button
- Pause button

SETTINGS:
Add:

- Sound toggle
- Vibration toggle
- Theme selector
- Reset progress option

IMPROVE THE ORIGINAL GAME:
Do not make it a direct clone. Create a foundation where I can add:

- Multiple levels
- Missions/objectives
- Different board sizes
- Power-ups
- Skins/themes
- Achievements
- Daily challenges
- Leaderboards
- In-app purchases later

CODE QUALITY:
- Use clean TypeScript types
- Avoid unnecessary dependencies
- Use reusable components
- Write readable production-quality code
- Add comments explaining important game logic
- Ensure the project runs with:

npm install
npx expo start

MIGRATION PROCESS:
First analyze the existing 2048 source code:
1. Identify the game logic.
2. Extract reusable algorithms.
3. Rewrite them in TypeScript.
4. Build the React Native UI.
5. Connect the game engine with the UI.
6. Test all swipe directions and merging behavior.

The final result should be a production-ready Expo mobile game that can be published to Google Play Store and Apple App Store.